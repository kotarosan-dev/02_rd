# CRM×AI Matching PoC - Environment Variables Template
# Copy this file to .env and fill in your actual values.
# Zoho 変数は zoho-api-access スキルの .env テンプレート（シンプル構成）に準拠。

# === Zoho API Credentials (Self Client) ===
# 取得先: https://api-console.zoho.com/ > Self Client
# ※ Refresh Token 不要。Client Credentials Flow でアクセス。
ZOHO_CLIENT_ID=1000.XXXXXXXXXXXXXXXXXX
ZOHO_CLIENT_SECRET=XXXXXXXXXXXXXXXXXXXXXXXX

# === Organization ID (数字のみ) ===
# 取得先: Zoho管理画面 > プロフィール画像 > 組織名 > Org ID
# demo3: 90001202404
ZOHO_ORG_ID=XXXXXXXXXXXX

# === Data Center ===
# jp / us / eu / in / au / ca（小文字で指定）
ZOHO_DC=jp

# OpenAI（マッチング理由生成に使用。未設定の場合は理由は表示されない）
OPENAI_API_KEY=sk-your-openai-api-key
# OPENAI_MODEL=gpt-4o-mini

# Pinecone
PINECONE_API_KEY=your-pinecone-api-key
PINECONE_HOST=https://your-index.svc.pinecone.io
PINECONE_INDEX=your-index-name
