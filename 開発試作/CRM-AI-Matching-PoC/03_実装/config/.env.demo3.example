# demo3 環境専用（組織ID 90001202404）
# このファイルをコピーして .env.demo3 を作成し、Client ID / Secret を記入すること。
# 実行: python setup_crm_modules.py --env demo3

# === Zoho API Credentials (demo3 用 Self Client) ===
ZOHO_CLIENT_ID=1000.XXXXXXXXXXXXXXXXXX
ZOHO_CLIENT_SECRET=XXXXXXXXXXXXXXXXXXXXXXXX

# === demo3 組織ID（固定）===
ZOHO_ORG_ID=90001202404

# === Data Center ===
ZOHO_DC=jp

# OpenAI / Pinecone は共通でよい場合は .env の値を流用可能（このファイルに書かなければスクリプトは .env を読まないので、ここに同じ値を書くか、.env を読んだあとで .env.demo3 で上書きする運用）
OPENAI_API_KEY=sk-your-openai-api-key
PINECONE_API_KEY=your-pinecone-api-key
PINECONE_HOST=https://your-index.svc.pinecone.io
PINECONE_INDEX=your-index-name
